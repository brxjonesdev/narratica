import { Character } from '@/entities/Character'
import { Card, CardHeader } from '@/presentation/components/ui/card'
import React from 'react'

export default function CharacterRelationships({ character }: { character: Character }) {
  return (
    <section className=' bg-white/5 h-full rounded-xl'>
    </section>
  )
}
